Ingest text files into the Pinecone vector database using Gemini Embedding 2.

Run the following command using the Bash tool:

```
cd "c:/ai/claudecode/embedingdemo" && "C:/Users/oliva/AppData/Local/Programs/Python/Python312/python.exe" ingest.py text --source "$ARGUMENTS"
```

If no argument is provided, ask the user for a file or folder path to ingest.

After running, report how many chunks were upserted and confirm success.
